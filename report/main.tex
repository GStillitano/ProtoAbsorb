% =====================================================================
%  Geometry of Open-Set Test-Time Adaptation
%  Compiles with pdfLaTeX (Overleaf / Moodle). No external .bib needed.
% =====================================================================
\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[margin=2.5cm]{geometry}
\usepackage{amsmath,amssymb,bm}
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{booktabs}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}
\usepackage{microtype}

% Figures: works whether main file sits at repo root or in report/.
\graphicspath{{figures/}{../figures/}{./}}

\captionsetup{font=small,labelfont=bf}

% --- notation macros (kept general and consistent) ------------------
\newcommand{\csid}{\textsc{cs}ID}
\newcommand{\csood}{\textsc{cs}OOD}
\newcommand{\D}{\mathcal{D}}
\newcommand{\g}{g_\theta}
\newcommand{\norm}[1]{\lVert #1 \rVert}
\newcommand{\method}[1]{\textsc{#1}}
\newcommand{\R}{\mathbb{R}}
\DeclareMathOperator{\softmax}{softmax}
\DeclareMathOperator*{\argmax}{arg\,max}
\DeclareMathOperator{\AUROC}{AUROC}
\DeclareMathOperator{\diag}{diag}
\newcommand{\E}{\mathbb{E}}
\newcommand{\Ind}{\mathbf{1}}
\newcommand{\Energy}{E}
\newcommand{\gz}{g_{\theta_0}}
\newcommand{\gt}{g_{\theta_t}}

\title{\textbf{The Geometry of Open-Set Test-Time Adaptation}}

\author{Leonardo Sani\\[2pt]
\small Sapienza University of Rome \\
\small \texttt{sani.2059860@studenti.uniroma1.it}}

\date{}

\begin{document}
\maketitle

% =====================================================================
\begin{abstract}
Test-Time Adaptation (TTA) keeps a deployed model accurate under
distribution shift. In the open world its success hinges on a second
requirement that is often ignored: the model must keep separating
in-distribution inputs from semantically novel out-of-distribution (OOD)
inputs. Existing open-set methods rely on high-confidence detection and
hard thresholds, and do not act on the feature norm directly, even though
the norm is the quantity adaptation moves most. We give a geometric
account of why the canonical entropy-based method (\method{Tent}) is
structurally at odds with OOD detection: with a frozen head, entropy
minimisation inflates feature norms uniformly across populations,
collapsing the norm gap that energy scores read. As our contribution we
introduce \method{Cassano}, a soft open-set TTA framework built around a
frozen, norm-invariant alignment score and a feature-norm objective:
likely-ID samples are adapted by entropy minimisation, likely-OOD samples
are regularised by an $L_1$ penalty on the feature norm that preserves the
OOD signal lost under covariate shift. On CIFAR-10-C, \method{Cassano}
reverses the collapse---energy-score AUROC rises over the stream where
\method{Tent} degrades---at no meaningful accuracy cost.
\end{abstract}

% --- teaser figure (non-floating: stays under the abstract) ----------
\begin{center}
  \includegraphics[width=0.9\linewidth]{teaser.jpeg}
  \captionof{figure}{\textbf{Open-set TTA.} Entropy minimisation inflates
  feature norms uniformly, collapsing the \csid/\csood{} gap that energy
  scores read; \method{Cassano} suppresses likely-OOD norms and preserves
  the separation.}
  \label{fig:teaser}
\end{center}

% =====================================================================
\section{Introduction}

Test-Time Adaptation (TTA) updates a deployed model online, without
labels, as the test stream drifts from the training distribution. The
canonical method, \method{Tent}~\cite{tent}, minimises predictive entropy
batch by batch, updating only the affine parameters of the
batch-normalisation (BN) layers.

Realistic streams are \emph{open-set}: alongside corrupted known-class
inputs (\emph{covariate-shifted in-distribution}, \csid) they contain
semantically novel ones (\emph{covariate-shifted out-of-distribution},
\csood) that the model should reject. The prevailing remedy is
\emph{split-and-oppose}: estimate per-sample membership from a score,
minimise entropy on the presumed \csid{} subset, oppose it on the
presumed \csood{} subset.

This framing hides a geometric failure present even with a perfect split.
With the head frozen, entropy minimisation acts only on the feature norm
and the angular alignment to the class weights. The norm dominates, and
BN affine parameters scale features uniformly: \csood{} norms inflate in
lockstep with \csid{} norms, collapsing the gap that energy scores read.
Detectability degrades regardless of split quality.

\paragraph{Contributions.}
\begin{itemize}\itemsep2pt
  \item \textbf{Protocol.} A two-phase protocol that tracks OOD and
  geometric metrics over the adaptation stream, isolating persistent
  affine drift from transient BN statistics.
  \item \textbf{Analysis.} A geometric account of \emph{why} closed-set
  TTA degrades open-set performance: norm inflation, gap collapse,
  angular relabelling.
  \item \textbf{Method.} \method{Cassano}, a soft open-set TTA framework
  derived from the analysis, which empirically reverses the AUROC
  collapse.
\end{itemize}

% =====================================================================
\section{Related Work}

\paragraph{Test-Time Adaptation.} \method{Tent}~\cite{tent} established
entropy minimisation over BN affine parameters as a lightweight baseline;
\emph{BN Adapt}~\cite{bnadapt} is the gradient-free special case.
\method{UniEnt}~\cite{unient} extends TTA to the open set with a hard
ID/OOD split and opposing per-subset objectives. We share the goal but
locate the failure in the geometry of the affine update, not the split.

\paragraph{OOD detection.} Energy-based scoring~\cite{energy} reads a
log-partition energy off the logits (Eq.~\eqref{eq:energy}); its power
rests on an \csid/\csood{} gap in logit magnitude, dominated by the
feature norm. We use the energy-score AUROC \eqref{eq:auroc} as the
detectability metric.

\paragraph{Feature geometry.} The feature norm is a known ID/OOD
discriminator~\cite{featurenorm}: OOD inputs tend to smaller-norm
embeddings. Relative representations~\cite{rrzs} encode a sample by its
cosine similarities to fixed anchors and are invariant to
rescaling---precisely what a norm-dominant adaptation leaves untouched.
This motivates our norm-invariant alignment score.

% =====================================================================
\newpage
\section{TTA in the Closed and Open Set}\label{sec:tta}

\paragraph{Test-time adaptation.} A source-trained model loses accuracy
under covariate shift (noise, blur, weather). TTA closes this gap
\emph{online}, with no labels and no source data, updating only a small
subset $\theta\subset\Theta$ of the network parameters.

\paragraph{Entropy minimisation (\method{Tent}).} The model sees an
unlabelled stream of batches
$\mathcal{B}_1\!\to\!\mathcal{B}_2\!\to\!\cdots$ of size $N$
(Fig.~\ref{fig:stream}).

% --- stream figure (non-floating: stays right here) ------------------
\begin{center}
  \begin{tikzpicture}[>=stealth]
    \tikzset{batch/.style={draw, rounded corners=2pt, fill=blue!5,
             minimum width=1.0cm, minimum height=0.6cm}}
    % batch stream
    \node[batch] (b1) at (0,0)   {\small $\mathcal{B}_1$};
    \node[batch] (b2) at (1.9,0) {\small $\mathcal{B}_2$};
    \node[batch] (b3) at (3.8,0) {\small $\mathcal{B}_3$};
    \node        (bd) at (5.4,0) {\small $\cdots$};
    \node[batch] (bt) at (7.0,0) {\small $\mathcal{B}_t$};
    \node        (be) at (8.5,0) {\small $\cdots$};
    \draw[->] (b1) -- (b2);
    \draw[->] (b2) -- (b3);
    \draw[->] (b3) -- (bd);
    \draw[->] (bd) -- (bt);
    \draw[->] (bt) -- (be);
    % parameter trajectory
    \node (t0) at (-1.7,-1.25) {\small $\theta_0$};
    \node (t1) at (0,-1.25)    {\small $\theta_1$};
    \node (t2) at (1.9,-1.25)  {\small $\theta_2$};
    \node (t3) at (3.8,-1.25)  {\small $\theta_3$};
    \node (td) at (5.4,-1.25)  {\small $\cdots$};
    \node (tt) at (7.0,-1.25)  {\small $\theta_t$};
    \node (te) at (8.5,-1.25)  {\small $\cdots$};
    \draw[->] (t0) -- (t1);
    \draw[->] (t1) -- (t2);
    \draw[->] (t2) -- (t3);
    \draw[->] (t3) -- (td);
    \draw[->] (td) -- (tt);
    \draw[->] (tt) -- (te);
    % entropy-gradient steps
    \draw[->, dashed, gray] (b1) -- (t1);
    \draw[->, dashed, gray] (b2) -- (t2);
    \draw[->, dashed, gray] (b3) -- (t3);
    \draw[->, dashed, gray] (bt) -- (tt);
    \node[gray, anchor=west] at (0.12,-0.65)
      {\small Eq.~\eqref{eq:tent}};
  \end{tikzpicture}
  \captionof{figure}{\textbf{The adaptation stream.} Unlabelled batches
  arrive online; each takes one entropy-gradient step \eqref{eq:tent} on
  the BN affine parameters, which accumulate as
  $\theta_0\!\to\!\cdots\!\to\!\theta_t$ while batch statistics are
  recomputed and discarded.}
  \label{fig:stream}
\end{center}

\noindent A $K$-class
classifier maps an input $x$ to logits $\psi(x)\in\R^K$ and a predictive
distribution
\begin{equation}\label{eq:softmax}
p(x)=\softmax\big(\psi(x)\big),
\qquad
p_{k}(x)=\frac{e^{\psi_k(x)}}{\sum_{j=1}^{K}e^{\psi_j(x)}} .
\end{equation}
\method{Tent}~\cite{tent} adapts \emph{only} the affine scale and shift
of the batch-normalisation layers,
$\theta=(\bm{\gamma},\bm{\beta})\subset\Theta$, acting channel-wise as
\begin{equation}\label{eq:bn}
\mathrm{BN}(z)=\bm{\gamma}\odot
\frac{z-\bm{\mu}_{\mathcal{B}}}{\bm{\sigma}_{\mathcal{B}}}+\bm{\beta},
\end{equation}
with statistics $(\bm{\mu}_{\mathcal{B}},\bm{\sigma}_{\mathcal{B}})$
recomputed per batch and never persisted; only $(\bm{\gamma},\bm{\beta})$
accumulate, the head and convolutional weights stay frozen. Each batch
takes one gradient step on the mean predictive entropy,
\begin{equation}\label{eq:tent}
\theta_t=\theta_{t-1}-\eta\,\nabla_{\theta}\,
\frac{1}{N}\sum_{x\in\mathcal{B}_t}H\big(p(x)\big),
\qquad
H(p)=-\sum_{k=1}^{K}p_k\log p_k,
\end{equation}
sharpening $p$ on the shifted inputs and recovering most of the accuracy
lost to corruption.

\paragraph{Closed vs.\ open set.} Each input is a two-component mixture,
\csid{} with probability $1-\alpha$ and \csood{} (unknown classes) with
probability $\alpha$,
\begin{equation}\label{eq:mixture}
x\sim(1-\alpha)\,P_{\mathrm{ID}}+\alpha\,P_{\mathrm{OOD}},
\qquad \alpha\in[0,1].
\end{equation}
$\alpha=0$ is the \emph{closed set}, \method{Tent}'s design regime.
For $\alpha>0$ the \csood{} inputs must be \emph{rejected}
(\S\ref{sec:metrics}), yet every batch \method{Tent} adapts on is itself
a mixture: the entropy step \eqref{eq:tent} sharpens \csood{} as well.
Since standard OOD scores---confidence, entropy, energy---read exactly
the peakedness entropy minimisation drives up, detection erodes.
\S\ref{sec:failure} makes this precise.

% =====================================================================
\newpage
\section{Understanding the Failure of \method{Tent}}\label{sec:failure}

\subsection{Last-layer geometry}\label{sec:background}

Let $\g:\mathcal{X}\to\R^d$ be the penultimate embedding and
$\mathbf{W}=[\mathbf{w}_1,\dots,\mathbf{w}_K]^\top\in\R^{K\times d}$ the
frozen linear head with bias $\mathbf{b}$, so that
\begin{equation}\label{eq:logits-def}
\psi(x)=\mathbf{W}\g(x)+\mathbf{b},
\qquad
\psi_k(x)=\mathbf{w}_k^\top\g(x)+b_k .
\end{equation}
Each $\mathbf{w}_k$ is the normal of the class-$k$ decision hyperplane;
the prediction is $c^*(x)=\argmax_k\psi_k(x)$. The winning logit in polar
form gives the decomposition that organises the analysis,
\begin{equation}\label{eq:logit}
\psi_{c^*}(x)=
\underbrace{\norm{\mathbf{w}_{c^*}}\,\norm{\g(x)}\,\cos\theta_{c^*}}
_{\text{norm}\,\times\,\text{alignment}}+\,b_{c^*},
\qquad
\cos\theta_{c^*}=\frac{\mathbf{w}_{c^*}^\top\g(x)}
{\norm{\mathbf{w}_{c^*}}\,\norm{\g(x)}} .
\end{equation}
With $\mathbf{W},\mathbf{b}$ frozen, the only levers entropy minimisation
can pull on $\psi_{c^*}$ are the feature norm $\norm{\g(x)}$ and the
alignment $\cos\theta_{c^*}$ (Fig.~\ref{fig:geometry}).

% --- geometry figure (non-floating: stays right here) ----------------
\begin{center}
  \begin{tikzpicture}[>=stealth, scale=1.15]
    % positive half-space
    \fill[blue!5] (-2.6,0) rectangle (2.6,2.6);
    % decision hyperplane
    \draw[thick] (-2.6,0) -- (2.6,0);
    \node[below] at (1.6,0)
      {\small $\{\,z:\ \mathbf{w}_{c^*}^{\top}z+b_{c^*}=0\,\}$};
    \node at (-2.3,0.25) {\small $+$};
    \node at (-2.3,-0.25) {\small $-$};
    % normal vector
    \draw[->, very thick, blue!60!black] (0,0) -- (0,2.2)
      node[above] {\small $\mathbf{w}_{c^*}$};
    % feature vector
    \draw[->, very thick, red!65!black] (0,0) -- (50:2.4)
      node[right] {\small $\g(x)$};
    % projection onto the normal
    \draw[dashed, gray] (50:2.4) -- (0,1.84);
    \draw[gray] (-0.05,1.84) -- (0.05,1.84);
    \node[left] at (-0.05,1.84)
      {\small $\norm{\g(x)}\cos\theta_{c^*}$};
    % angle
    \draw (0,0.6) arc (90:50:0.6);
    \node at (70:0.85) {\small $\theta_{c^*}$};
  \end{tikzpicture}
  \captionof{figure}{\textbf{Last-layer geometry.} The frozen head fixes
  the class-$c^*$ hyperplane and its normal $\mathbf{w}_{c^*}$. The
  winning logit \eqref{eq:logit} is the projection
  $\norm{\g(x)}\cos\theta_{c^*}$, scaled by $\norm{\mathbf{w}_{c^*}}$:
  adaptation can only stretch the feature ($\norm{\g(x)}\!\uparrow$) or
  close the angle ($\theta_{c^*}\!\downarrow$).}
  \label{fig:geometry}
\end{center}

\subsection{OOD detection and entropy}\label{sec:ood}

Post-hoc OOD scores read the peakedness entropy measures. Confidence,
\begin{equation}\label{eq:conf}
C(x)=\max_k p_{k}(x),
\end{equation}
and energy~\cite{energy}, the log-partition of the logits,
\begin{equation}\label{eq:energy}
\Energy(x)=-\log\sum_{k=1}^{K}e^{\psi_k(x)},
\end{equation}
are both monotone in the predictive entropy $H(p(x))$. A detector
$s(x)=-\Energy(x)$ flags low-entropy inputs as \csid; it works only
because \csid{} sits at lower entropy than \csood,
\begin{equation}\label{eq:ent-gap}
\E_{P_{\mathrm{OOD}}}H\big(p(x)\big)>\E_{P_{\mathrm{ID}}}H\big(p(x)\big),
\end{equation}
\method{Tent} minimises $H$ over the whole mixed batch \eqref{eq:tent},
\csood{} included, driving $\E_{P_{\mathrm{OOD}}}H$ toward
$\E_{P_{\mathrm{ID}}}H$: the gap closes and detectability collapses.

\subsection{Predicted effect of \method{Tent}}\label{sec:predict}

Energy and confidence grow with the winning logit $\psi_{c^*}(x)$, and
detection works because \csid{} reaches a larger one than \csood. By the
decomposition \eqref{eq:logit}, with $\mathbf{W},\mathbf{b}$ frozen,
entropy minimisation \eqref{eq:tent} can raise $\psi_{c^*}$ through only
two levers: \emph{norm inflation} ($\norm{\g(x)}\!\uparrow$) and
\emph{re-alignment} ($\cos\theta_{c^*}\!\uparrow$). Each step pulls these
levers on \csood{} as much as on \csid, since the batch-normalisation
update is shared across the whole batch.

% =====================================================================
\section{Experiment on the Failure of \method{Tent}}\label{sec:experiment}

\subsection{Time-tracking protocol}\label{sec:protocol}

We separate adaptation from evaluation so that any change in a metric
reflects only the persistent affine drift.

\paragraph{Phase 1 (adaptation).} Run a method on a stream of $T$ batches
and save the BN affine state $\theta_t$ after each step, yielding $T{+}1$
checkpoints $\theta_0,\dots,\theta_T$.

\paragraph{Phase 2 (analysis).} Load each checkpoint, freeze it, and
evaluate it on a fixed held-out diagnostic set
$\D=\D_{\csid}\cup\D_{\csood}$ (2000$+$2000 examples) that is never adapted
on. Any change in a trajectory $m(0),\dots,m(T)$ then reflects only the
drift $\theta_{t-1}\!\to\!\theta_t$. The diagnostic set is evaluated in
$20$ fixed mixed batches of $100\,\csid+100\,\csood$---the same size and
ID/OOD regime as adaptation---so BN statistics are computed under
deployment-realistic conditions. A single large batch would change the
effective feature space; evaluating the populations separately would let
each be normalised by its own statistics and artificially stabilise the
\csood{} features, masking the norm-gap collapse we measure.

\paragraph{Setup.} A stream is fixed by
$(\text{method},\text{corruption},\text{\csood source},\text{open\_set},
\text{seed})$. Unless noted we use CIFAR-10-C \texttt{gaussian\_noise} at
severity~$5$ as \csid{} and SVHN-C as \csood, balanced open-set, $N{=}200$,
$T{=}80$ with no repeated examples, seed~$0$. The backbone is
WideResNet-40-2 pretrained with AugMix+JSD~\cite{augmix} on CIFAR-10
(RobustBench~\cite{robustbench}, clean accuracy $\approx 95.8\%$).

\subsection{Metrics}\label{sec:metrics}

Every metric is a function of a frozen checkpoint $\theta_t$ and the fixed
diagnostic set; $\E_{\D_\bullet}$ denotes a mean over a population
$\bullet\in\{\csid,\csood\}$, and $c^*_0(x)=\argmax_k\psi_0(x)$ is the
\emph{source}-predicted class, frozen at $t{=}0$.

\begin{itemize}\itemsep4pt
\item \textbf{Detectability and accuracy.} Detectability is the
separability of the two populations under a score $s:\mathcal{X}\to\R$,
\begin{equation}\label{eq:auroc}
\AUROC(s)=\Pr\big[\,s(X_{\csid})>s(X_{\csood})\,\big],
\end{equation}
the Mann--Whitney probability that an \csid{} input outscores an \csood{}
one, estimated over $\D_{\csid}\times\D_{\csood}$; we use $s=-\Energy$ and
write $\AUROC(t)$ for the frozen checkpoint $\theta_t$. Accuracy is
$\mathrm{Acc}(t)=\E_{\D_{\csid}}\Ind\!\big[c^*_t(x)=y\big]$.

\item \textbf{Feature norms and gap.} Mean $L_2$ and $L_1$ norms per
population, $\E_{\D_\bullet}\norm{\gt(x)}_2$ and
$\E_{\D_\bullet}\norm{\gt(x)}_1$, and the \csid$-$\csood{} \emph{norm gap}
\begin{equation}\label{eq:gap}
\Delta(t)=\E_{\D_{\csid}}\norm{\gt(x)}-\E_{\D_{\csood}}\norm{\gt(x)}
\end{equation}
($\Delta_2,\Delta_1$ for each norm), whose positivity the energy detector
depends on.

\item \textbf{Alignment.} Cosine to the \emph{source}-predicted class,
$\E_{\D_\bullet}\!\big[\cos\theta_{c^*_0(x)}\big]$, and the norm-invariant
max cosine $\E_{\D_\bullet}\!\big[\max_k \cos\angle(\gt(x),\mathbf{w}_k)\big]$
(the score of Eq.~\eqref{eq:score}).

\item \textbf{Semantics and confidence.} Mean distance to the nearest
frozen source centroid,
\begin{equation}\label{eq:centroid}
\E_{\D_\bullet}\min_{c}\norm{\gt(x)-\bm{\mu}_c^{(0)}},
\qquad
\bm{\mu}_c^{(0)}=\E\big[\gz(x)\mid y=c\big]\ \text{(clean CIFAR-10)};
\end{equation}
the \csood{} confidence $\E_{\D_{\csood}}C(x)$ from \eqref{eq:conf}; and the
predicted-class churn $\E_{\D_{\csood}}\Ind\!\big[c^*_t(x)\neq c^*_{t-1}(x)\big]$.
\end{itemize}

\noindent At $t{=}0$ we also report the fraction of each population on the
positive side of its predicted hyperplane,
$F_\bullet=\E_{\D_\bullet}\Ind\!\big[\cos\theta_{c^*_0(x)}>0\big]$, which
conditions the reading of the angular metrics.

\subsection{Results}\label{sec:failure-results}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.72\textwidth]{tent/gaussian_noise_svhn_c_open_seed0/geometry1.png}
  \caption{\textbf{Norm inflation and gap collapse} under \method{Tent}.
  \emph{Left:} mean $L_2$ (top) and $L_1$ (bottom) feature norm per
  population (band $=$ IQR); both rise. \emph{Right:} the \csid$-$\csood{}
  norm gap shrinks toward zero (and the $L_1$ gap goes negative) around
  $t\!\approx\!25$ before over-shooting---the window where energy scoring
  degrades most. The norm is not a clean signal in our open-set setting:
  it moves under adaptation and even inverts.}
  \label{fig:geo1}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.72\textwidth]{tent/gaussian_noise_svhn_c_open_seed0/geometry2.png}
  \caption{\textbf{Angular relabelling, not absorption} under
  \method{Tent}. \emph{Top:} cosine to the source-predicted class (left)
  drops for \csood{} while the max cosine over all class weights (right)
  rises---\csood{} re-aligns to its nearest weight. \emph{Bottom:}
  distance to the nearest frozen centroid stays flat (left) while \csood{}
  max-softmax confidence climbs (right). No semantic absorption, only
  confidence inflation that the entropy objective rewards.}
  \label{fig:geo2}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.72\textwidth]{tent/gaussian_noise_svhn_c_open_seed0/maxcos_dist_t0.png}
  \caption{\textbf{The frozen alignment score separates the populations
  at $t{=}0$.} Density of the max cosine to the class weights for \csid{}
  (centre $\approx0.54$) versus \csood{} ($\approx0.40$). This statistic is
  norm-invariant and, computed by a frozen model, stationary across $t$.}
  \label{fig:maxcos}
\end{figure}

The diagnostics confirm the prediction of \S\ref{sec:predict} and add
detail. \emph{(i) Norm inflation is uniform and the gap collapses}
(Fig.~\ref{fig:geo1}): both populations' norms rise, the \csid$-$\csood{}
gap shrinks toward zero around $t\!\approx\!25$, and the $L_1$ gap goes
\emph{negative}---\csood{} features become larger in $L_1$ than \csid{}
ones. The feature norm is therefore not a reliable signal in our setting:
adaptation moves it and can invert it. \emph{(ii) Adaptation is angular
relabelling, not absorption} (Fig.~\ref{fig:geo2}): the cosine to the
source-predicted class drops for \csood{} while the max cosine over all
weights rises; \csood{} samples are re-aligned to whichever weight is
nearest, with confidence climbing, while centroid distances stay flat.

Two facts survive the analysis and motivate the method. First, at $t{=}0$
the \emph{frozen} max-cosine statistic already separates the populations
(Fig.~\ref{fig:maxcos}). Second, this statistic is norm-invariant and,
computed by a frozen model, stationary across $t$---immune to the very norm
inflation that destroys energy scoring.

% =====================================================================
\section{Our Proposal: \method{Cassano}}\label{sec:cassano}

\subsection{A general framework}\label{sec:framework}

We cast open-set TTA as a pipeline
\[
s(x)\;\longrightarrow\;\pi(\mathrm{OOD}\mid x)\;\longrightarrow\;
w_i\;\longrightarrow\;\mathcal{L}.
\]
A score $s(x)$ yields a posterior $\pi(\mathrm{OOD}\mid x)$, which is turned
into a per-sample membership weight---hard or soft---
\begin{equation}\label{eq:label}
w_i=
\begin{cases}
\Ind\!\big[\pi(\mathrm{OOD}\mid x_i)>\tau\big] & \text{(hard split)},\\[3pt]
\pi(\mathrm{OOD}\mid x_i)\in[0,1] & \text{(soft weight)},
\end{cases}
\end{equation}
and $w_i$ gates an objective that adapts on \csid{} and opposes on \csood.
\method{UniEnt} instantiates this with an energy score and a hard split;
\method{Cassano} with a frozen alignment score and a soft weight, each
choice dictated by \S\ref{sec:failure}.

\subsection{Method}\label{sec:method}

\paragraph{Score: frozen, norm-invariant alignment.} A \emph{frozen} scorer
(original weights $\theta_0$, BN batch statistics, no gradient) runs
alongside the adapted model and produces the max cosine to the class
weights,
\begin{equation}\label{eq:score}
s_i=\max_k \frac{\g(x_i)\cdot\mathbf{w}_k}
{\norm{\g(x_i)}\,\norm{\mathbf{w}_k}} .
\end{equation}
This is the one statistic the analysis showed to be both discriminative at
$t{=}0$ (Fig.~\ref{fig:maxcos}) and immune to adaptation: it is
norm-invariant, so it ignores the inflation of finding~(i), and it is
computed by a frozen model, so scores pooled across steps are unbiased---unlike
the energy score, which is corrupted by the very adaptation it would
supervise.

\paragraph{Posterior: soft labelling.} Pool the scores seen up to step $t$,
$\mathcal{S}_t=\bigcup_{\tau\le t}\{s_i\}_{i\in\mathcal{B}_\tau}$, and fit by
EM a two-component Gaussian mixture
\begin{equation}\label{eq:gmm}
p(s)=\sum_{c\in\{\mathrm{id},\mathrm{ood}\}}
\pi_c\,\mathcal{N}\!\big(s\mid\mu_c,\sigma_c^2\big),
\qquad
\mu_{\mathrm{id}}>\mu_{\mathrm{ood}} ,
\end{equation}
the higher-mean component being ID. Because the scorer is frozen, the
score law is stationary across $t$, so pooling in \eqref{eq:gmm} is
unbiased and the fit sharpens as $|\mathcal{S}_t|$ grows. The detached Bayes
posterior gives the soft weight \eqref{eq:label},
\begin{equation}\label{eq:post}
\pi(\mathrm{OOD}\mid x_i)=
\frac{\pi_{\mathrm{ood}}\,\mathcal{N}(s_i\mid\mu_{\mathrm{ood}},\sigma_{\mathrm{ood}}^2)}
{\sum_c \pi_c\,\mathcal{N}(s_i\mid\mu_c,\sigma_c^2)},
\qquad \pi_{\mathrm{ID}}=1-\pi_{\mathrm{OOD}},
\end{equation}
used as a weight, never optimised---avoiding the hard-threshold errors that
the split-and-oppose recipe propagates into its objective.

\paragraph{Objective: oppose on the failure lever.} The two findings
dictate the two terms:
\begin{equation}\label{eq:loss}
\mathcal{L}=\frac{1}{N}\sum_i
\Big[\,\pi_{\text{ID}}(x_i)\,H(p_i)
\;+\;\pi_{\text{OOD}}(x_i)\,\lambda\,\norm{\g(x_i)}_1\,\Big].
\end{equation}
On likely-\csid{} samples we minimise entropy---the useful part of
\method{Tent}. On likely-\csood{} samples we penalise the $L_1$ feature
norm. The shared affine update acts as a channel-wise \emph{diagonal
scaling}, to first order
\begin{equation}\label{eq:scaling}
\gt(x)\approx\mathbf{D}_t\,\gz(x),
\qquad
\mathbf{D}_t=\diag(\bm{d}_t),\ \ \bm{d}_t>\bm{0},
\end{equation}
inflating every sample's norm; acting directly on this lever, rather than
hoping a hard split prevents norm inflation, we suppress \csood{} norms and
re-open the gap \eqref{eq:gap}. Since $H\le\ln 10\approx2.30$ while
$\norm{\g}_1\approx 28$--$30$, we set $\lambda\approx 1/30$ to balance the
two terms. While the pool $\mathcal{S}_t$---and hence the mixture
\eqref{eq:gmm}---is still small, the posterior is noisy; we damp it with a
learning-rate warm-up over the first $K$ steps,
\begin{equation}\label{eq:warmup}
\eta_t=\eta\cdot r(t/K),\qquad
r(u)=
\begin{cases}
u & \text{linear},\\[2pt]
\tfrac12\big(1-\cos\pi u\big) & \text{cosine},\\[2pt]
\dfrac{1-e^{-u/\tau}}{1-e^{-1/\tau}} & \text{exponential},
\end{cases}
\qquad r(u\!\ge\!1)=1 .
\end{equation}

\subsection{Results: \method{Cassano} vs.\ \method{Tent}}\label{sec:results}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.92\textwidth]{gaussian_noise_svhn_c_open_seed0/exp1_auroc.png}
  \caption{\textbf{Open-set TTA over the stream}
  (CIFAR-10-C \texttt{gaussian\_noise} vs.\ SVHN-C, $T{=}80$).
  \emph{Left:} energy-score AUROC. \method{Tent} (orange) degrades
  monotonically $0.79\!\to\!0.69$; \emph{BN Adapt} (blue) is flat at
  $0.79$, isolating the persistent affine update as the cause;
  \method{Cassano} (green) \emph{rises} $0.79\!\to\!0.94$. \emph{Right:}
  \csid{} accuracy. All adapting methods reach $\approx0.80$--$0.83$, so
  \method{Cassano} recovers detectability without sacrificing accuracy.}
  \label{fig:exp1}
\end{figure}

\begin{table}[t]
  \centering
  \small
  \caption{Endpoints of the trajectories in Fig.~\ref{fig:exp1}
  ($t{=}0\to t{=}80$). AUROC of the energy score; accuracy on
  $\D_{\csid}$.}
  \label{tab:endpoints}
  \begin{tabular}{lcc}
    \toprule
    Method & AUROC ($0\to T$) & Acc ($0\to T$)\\
    \midrule
    BN Adapt          & $0.79\to0.79$ & $0.77\to0.77$\\
    \method{Tent}     & $0.79\to\mathbf{0.69}$ & $0.77\to0.83$\\
    \method{Cassano}  & $0.78\to\mathbf{0.94}$ & $0.77\to0.80$\\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{AUROC vs.\ accuracy.} Figure~\ref{fig:exp1} and
Table~\ref{tab:endpoints} make the trade-off explicit. \method{Tent} buys
$+6$pt accuracy with a $-10$pt AUROC loss: the objectives are orthogonal,
and entropy minimisation optimises accuracy at the direct expense of
separability. \emph{BN Adapt} isolates the cause---with no gradient step
its AUROC is perfectly flat, so the degradation is entirely due to the
persistent $(\bm{\gamma},\bm{\beta})$ affine drift. \method{Cassano} inverts
the trend: penalising \csood{} norm not only halts the collapse but drives
AUROC \emph{above} the $t{=}0$ baseline to $0.94$, at negligible accuracy
cost.

\paragraph{Geometric reading.} The diagnostics of \S\ref{sec:failure}
explain the gap. \method{Tent} inflates \csood{} norms and relabels them
angularly; \method{Cassano}'s $L_1$ penalty keeps likely-\csood{} norms
suppressed, restoring the energy gap the detector reads. Against the hard
split-and-oppose family, \method{Cassano}'s advantage is twofold: its score
is frozen and norm-invariant rather than corrupted by the adaptation it
supervises, and its labelling is a soft posterior rather than a hard
threshold, so \eqref{eq:loss} degrades gracefully under split uncertainty
instead of compounding split errors.

% =====================================================================
\section{Conclusions and Discussion}

Closed-set TTA and open-set robustness pull in opposite geometric
directions: with a frozen head, entropy minimisation over BN affine
parameters inflates feature norms uniformly, collapsing the energy gap that
OOD detection relies on. The dangerous degree of freedom is the
\emph{global} feature scale, and detectors that read it (energy, max-logit)
are self-defeating under adaptation. We made this concrete with a
time-tracking protocol and a geometric analysis, then used the analysis to
design \method{Cassano}, which supervises with a statistic the adaptation
cannot corrupt (a frozen, norm-invariant alignment score) and acts on the
exact lever the failure uses (the $L_1$ norm). \method{Cassano} reverses
the collapse---AUROC $0.79\!\to\!0.94$---at no meaningful accuracy cost.
Natural next steps are the full corruption sweep, a second \csood{} source
(Rome32), and a head-to-head against \method{UniEnt} under this protocol.

% =====================================================================
\begin{thebibliography}{9}
\small
\bibitem{tent} D.\ Wang, E.\ Shelhamer, S.\ Liu, B.\ Olshausen, T.\ Darrell.
\emph{Tent: Fully Test-Time Adaptation by Entropy Minimization.} ICLR 2021.

\bibitem{bnadapt} S.\ Schneider et al.
\emph{Improving Robustness against Common Corruptions by Covariate Shift
Adaptation.} NeurIPS 2020.

\bibitem{unient} Z.\ Gao et al.
\emph{Unified Entropy Optimization for Open-Set Test-Time Adaptation.}
CVPR 2024.

\bibitem{energy} W.\ Liu, X.\ Wang, J.\ Owens, Y.\ Li.
\emph{Energy-based Out-of-distribution Detection.} NeurIPS 2020.

\bibitem{featurenorm} J.\ Park, Y.\ G.\ Jung, A.\ B.\ J.\ Teoh.
\emph{Understanding the Feature Norm for Out-of-Distribution Detection.}
ICCV 2023.

\bibitem{rrzs} L.\ Moschella et al.
\emph{Relative Representations Enable Zero-Shot Latent Space
Communication.} ICLR 2023.

\bibitem{augmix} D.\ Hendrycks et al.
\emph{AugMix: A Simple Data Processing Method to Improve Robustness and
Uncertainty.} ICLR 2020.

\bibitem{robustbench} F.\ Croce et al.
\emph{RobustBench: A Standardized Adversarial Robustness Benchmark.}
NeurIPS Datasets \& Benchmarks 2021.
\end{thebibliography}

\end{document}
